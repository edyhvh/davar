name: Backend Deploy

on:
  workflow_dispatch:
  push:
    branches: [main, koyeb]
    paths: ["backend/**"]

jobs:
  backend_deploy:
    name: backend-deploy
    runs-on: ubuntu-latest
    concurrency:
      group: backend-deploy-${{ github.ref_name }}
      cancel-in-progress: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate TS2009 files exist in storage
        run: |
          # This would need Supabase CLI or API call to check bucket contents
          # For now, placeholder - in real implementation, add Supabase CLI setup
          echo "TS2009 storage validation placeholder"
          # TODO: Install Supabase CLI and check bucket has expected files

      - name: Notify deployment
        run: |
          echo "Backend deployment source-of-truth is Koyeb Git auto-deploy"
          echo "This workflow remains a policy and verification companion"
          echo "Current branch: ${{ github.ref_name }}"
          echo "Validate on branch 'koyeb' before tracking 'main' in Koyeb service settings"
