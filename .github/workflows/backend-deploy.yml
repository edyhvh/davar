name: Backend Deploy

on:
  workflow_dispatch:

jobs:
  backend_deploy:
    name: backend-deploy
    runs-on: ubuntu-latest

    steps:
      - name: Backend deployment deprecated
        run: |
          echo "Backend deployment workflow is intentionally manual and out of scope."
          echo "Web + mobile are the active GitHub Deployments environments."
