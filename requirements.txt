# Davar Backend Production Requirements
#
# This file is intentionally minimal so that Koyeb's Heroku buildpack
# (which reads requirements.txt from the repo root) installs only the
# production dependencies needed to run the FastAPI backend.
#
# Development and data-processing tools live in requirements-dev.txt.

-r backend/requirements.txt
